// performing a task 
function greet_2(name, lastName) {
    console.log('hello' + name + ' ' + lastaName);
}

//Calculating a value
function square(number) {
    return number * number;
}

square(2);
//or
let number = square(2)
console.log(number);
//or
console.log(square(2));


//Console.Log is also a kind of Function as it uses '()' and passing arguments like functions or any Variable